{% extends 'base.html.twig' %}

{% block title %}Participantes index{% endblock %}

{% block body %}
    <h1>Participantes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participante in participantes %}
            <tr>
                <td>{{ participante.id }}</td>
                <td>
                    <a href="{{ path('app_participantes_show', {'id': participante.id}) }}">show</a>
                    <a href="{{ path('app_participantes_edit', {'id': participante.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_participantes_new') }}">Create new</a>
{% endblock %}
