{% extends 'base.html.twig' %}

{% block title %}Participantes{% endblock %}

{% block body %}
    <h1>Participantes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participante.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_participantes_index') }}">back to list</a>

    <a href="{{ path('app_participantes_edit', {'id': participante.id}) }}">edit</a>

    {{ include('participantes/_delete_form.html.twig') }}
{% endblock %}
